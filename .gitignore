# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
*.egg-info/
*.egg

# Build artifacts
build/
dist/
wheels/

# assets/ 下的二进制 + geo + UI 直接 commit 进 git,让 clone 后能直接 build。
# 想升级版本跑 `scripts/fetch-assets.sh` 重拉一份再 commit。

# Virtual environments
.venv/
venv/
env/

# Editor / IDE
.vscode/
.idea/
*.swp
.DS_Store

# Local-only Claude Code settings (per-machine)
.claude/

# Runtime state (never check in)
*.log
cache.db
config.yaml
*.bak

# Decodo generated subscriptions/health snapshots may contain credentials.
decodo*.yaml
decodo*.yml
decodo-health.json
